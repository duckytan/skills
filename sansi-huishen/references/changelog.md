## v4.2.1（2026-09-07）· 自审修订：判据语义化 + 判据单源 + 备份身份

> 依据：`sanshi-selfaudit-v42-report.md` 自审（**自指命中** → 已引入外部独立视角）
> 主旨：v4.2 的「11/11 全 PASS」经自审判定为**自证式 tautology**（判定条件 100% 为 `grep -c ≥1`）。本版把验收从「词在不在」改为「内容对不对」。

- **判据语义化（V-1~V-7）**：全部由关键词计数升级为**段落内内容断言** —— V-2 要求 changelog 有**独立章节**（非字符串出现）；V-3 升为**字段级**（json + lastCheck + lastSha + lastVersion，且无旧字段）；V-4 要求落地闸门段内**同时**含硬约束 `> 5 条` + skip 模板 + 砍不动兜底；V-5/V-6/V-7 同理要求规则与兜底在位
- **判据单源**：原 `sansi-verify.sh`（v4.1 产物）**已确认丢失**，其 AU/INV/TGT 系列自本版起**并入** `sansi-verify-v42.sh`（R-5~R-8）。**全库仅一个验收入口，不得再建第二份**
- 新增 **R-8 行为断言**：真实执行 `update_check.py` 并校验退出码 + 状态文件（不再是「文件在就算过」）
- 新增 **gate 模式**：`bash sansi-verify-v42.sh gate <报告.md>`，解析「落地闸门结果：建议 X → 保留 Y」并断言 **Y ≤ 5** / **Y < X（真砍了）** / **X = Y + Z（算术自洽）**，使「闸门挡不挡得住」可机检
- **备份身份标记**（修 N2）：新增 `scripts/backup-skills.py`，目录名 `skills.bak-<ts>-from-v<前态>-to-v<目标>` + `_IDENTITY.txt`。备份可自证前后态 —— 旧版纯时间戳，曾出现「最新备份实为改后快照」的陷阱
- **协议变更单列补登记**（修 N3）：「自动下载覆盖」→「仅打印提示，不自动覆盖」由 v4.2 的附带半句提升为独立条目，与 SKILL.md 第 5 行自述一致
- **已知未修**：D12 术语混用（明辨「决」/ 破妄「诀」）→ 并入 Part B

## v4.2（2026-09-07）· 运行时硬化：双闸门 + 留白 + 协议对齐

> 依据：`sanshi-20260907-001` 真实项目实跑（审查对象 nihaixia）· 方案 `sanshi-upgrade-v4.2.md`
> 主旨：v4.1 解决了「有没有闸门」，v4.2 解决「闸门在硬场景挡不挡得住」与「三司是不是真在独立发现」。

- 新增**落地闸门**（阶段 4「整合报告 → 交付」转场）：建议 **> 5 条强制砍到 ≤ 5 条**，砍不动须写明理由；skip-detection 输出 `落地闸门结果：建议 X 条 → 保留 Y 条`。与 v4.1 动工前闸门合称**双闸门**（同一把刀的两个工位，不新增模块名）
- 新增**阶段 0 留白条款**：事实表**上限 9 条** · 禁止倾向性标注 · 三司**必填「主控事实表之外的发现」**（允许为空但须声明）· **边际发现率 < 20% 触发主控自检**（治「喂太满 → 三司退化成复核员」）
- 新增**知彼情报包最低标准**：≥ 2 个独立来源 + ≥ 1 个同类对象横向对照 + 时效标注 + 单源即标黄「知彼不充分」
- 新增**事实争议登记表**：司与司 / 司与事实表冲突须登记，**裁定依据须为可复现实查**（不可是推理）
- **自指命中由软要求升为强制**（v4.0.4 强化）：结论影响对象含三司会审本体 / 四诀 / 编排层 → 必须标注「自指命中」并引入外部视角。教训：sanshi-20260907-001 审出 U1 未标注
- **协议对齐（Q1=B）**：6 份 `update_check.py` 状态文件 `.last_check` → **`.update-check.json`**（`lastCheck` ISO8601 / `lastSha` / `lastVersion`），**恢复 commit sha 比对**（能检出「版本号未变但内容已变」）；协议文本「自动下载覆盖」改为**「仅打印提示，不自动覆盖」**，与实现对齐
- **版本治理**：标题 / 落款 / changelog 三处版本号统一为 **v4.2**；修复阶段 4 标题重复（D11）
- 新增机检项 **V-1~V-7**（接入 `sanshi-verify.sh`）：版本三处一致 / changelog 无断层 / 协议 6-6 对齐 / 双闸门 / 留白条款 / 知彼标准 / 裁定登记

## v4.1（2026-09-03）· 奥卡姆闸门 + 文化依据条款

- 新增**奥卡姆闸门**（阶段 2「制定计划 → 执行跟进」转场）：ponytail 7 级阶梯 + 老子「损之又损」双文化锚，为本体系**第一个西方锚**
- 确立**文化依据条款**（体系级）：根基 =「每个模块须有可追溯的思想源头」，**不是**「须有东方源头」；新增模块准入两问（有无真实源头 / 名实相符），**不问出身**
- 四诀补「真实思想锚」列：明辨 = 现代第一性原理审查（Kahneman / Pre-Mortem / OWASP / NIST）· 破妄 = 唐密·般若 · 五行 = 老子 · 知彼 = 孙武
- 截面数叙事 12 → 实测 **11** 对齐；破妄诀 11 截面 × 3 妄执 = **33** 法门（修正原「39」虚构数）
- 阶梯唯一权威源沉到 `powang-jue/references/ponytail-ladder.md`（防双源漂移）

## v4.0（2026-08-04）· 调度层改造（阶段 1a）

- v4.0.1 **机械化触发清单**（7 维 22 项）取代叙事表，治「实操 100% 走圆桌」的漂移
- v4.0.2 R1 先验测试强制落档（治自创铁律被绕过）
- v4.0.3 子 agent **首行回执协议**（治「形似神不似」）
- v4.0.4 自审外部视角（R11 · 治自指盲区）
- v4.0.5 **知彼诀串行前置时序**（治「侦察兵未归三司先打」）
- v4.0.6 共享 skill 单写者（真源 + 软链）
- v4.0.8 **触发词路由**（`scripts/trigger-router.py`，5 样本 100% 准确）
- 4 诀 scripts 补齐：明辨 `catch_incidents.py` / 破妄 `assumption-extractor.py` / 五行 `five-dim-quick.py` / 知彼 `internal-scan.py`

---

### v2.5（7-19 12:17 拍板 · 锡哥「初衷也要写进去」）

- 🆕 §🎯 设计初衷（锡哥 9:15 原文完整补入 · 必读）——之前 v2.x 只摘录片段，这次完整补 3 段原话（主控 / 会审 / 三司）
- 🔗 锡哥拍板初衷 = 本 skill 的设计灵魂，后续修订必读

### v2.4（7-19 12:15 拍板 · 锡哥自审后修订）

- 🔄 失败模式第 6 条「锡哥跑会审中途又问新问题」 → **锡哥明确优先才暂存，否则主控反问等拍板**（解六祖 PW-03「凭什么暂存」妄执）
- 🔄 v3.0 实验性「4 元规则重构」撤销，回退 v2.3 + 仅改 1 处 = v2.4

### v2.3（7-19 11:09 拍板 · 锡哥自审 3 P0 缺口后）

- 🆕 §1.3 多项任务逐项确认（防 PW-24 妄执复发）
- 🆕 §🚦 跳过模式（8 类任务判据 + 判定口诀）
- 🆕 §🚨 失败模式（7 类失败场景 + 主控响应）
- 🔄 接力棒加「任务开始时间 + 任务状态」字段
- 🔄 报告模板加「任务开始时间」+ 锡哥画像 + 6 方向印证表
- 🔗 关联 PW-24 教训文档

### v2.2（7-19 10:02 拍板）

- 🆕 §1.2 锡哥画像感知
- 🆕 三司代表语录 + 人物-模组对应表
- 🔄 Stage 2 单向链 → 网状 6 方向
- 🔄 Stage 3 触发 3 类 → 5 类

### v2.1 / v2.0 / v1.1 / v1.0（详见 v2.2 备份）

---

### v3.0（2026-07-25 00:45 錡哥拍板整改完成）

- 🆕 **P0-1** 修「48 模组虚构」：删除 22 处虚构编号（MB-XX / PW-XX / WX-XX），改为步骤名引用
- 🆕 **P0-5** 加「参与司判定规则」：明确三司参与条件 + 7 类任务场景机械判定表 + 化解「简化模式 vs 单司」矛盾
- 🆕 **P1-2** 砍状态机 + 变更日志常驻：137 行从主体移至 `references/state-machine.md` + `references/changelog.md`
- 🆕 **P2-7** 加「落地四问」闸门：默认/圆桌模式出报告前必过（用户/成本/风险/项目对齐）
- 🆕 建 `BRAND.md`：品牌调性常量（v1.0）
- 🆕 建 `references/project-constants.md`：项目常量指针（5 个真实常量 + 2 项失实核正）
- 🔄 SKILL.md 主体从 571 行 → 463 行（-19%）
- 📌 触发：錡哥 2026-07-24 23:47「用三司自审编排层」+ 23:55/23:57 两版整改方案
- 📌 倒逼：自述≠实态（22 处虚构编号）+ 内部矛盾（3 模式表 vs 单司降级）+ 常驻臃肿（v2.8 状态机 165 行不跑）
- 📌 诚实核正：錡哥 P2-7 v2 引用了 2 项不存在的项目常量（git 代理 127.0.0.1:12000 / 四大痛点）—— 已改用真实常量


### v3.1.2（2026-07-25 01:02 錡哥拍板加 sub-agent 升级）

- 🆕 **🎛 工作流执行机制选择**（~231 行·置于「🎯 设计初衷」之前）
  - **机制 A · 串行模拟**（轻量·默认）：1 进程切 3 视角
  - **机制 B · sub-agent 隔离**（重型·圆桌/重大决策）：3 独立 sub-agent,~1.4x token
  - **机制 C · 共享背景协议**（防盲化）：阶段 0 事实收集 + 事实表模板 + 排除项
  - **机制 D · 失败兜底**：单 sub-agent 失败 / 2+ 失败 / 超时 3 类
  - **R1 实测先验**：激活并行前必跑隔离可行性测试
- 🆕 **自动选择矩阵 5 档**：轻量（跳过）/ 中低（简化·A）/ 中（默认·A）/ 高（圆桌·B）/ 锡哥指定
- 🆕 **显式圆桌词不强制并行**（WorkBuddy B 坑）：隔离信号组合判定
- 🆕 **事中升级并行**（WorkBuddy B 坑）：防污染已发生时补救
- 🆕 **P0-P4 阶段**（避免与主流程阶段 1-4 混淆）：P0 准备 / P1 派发 / P2 比对 / P3 圆桌 / P4 整合
- 🆕 **网状印证协议**：主控驱动 sessions_send（避免 sub-agent 互查再污染）
- 🆕 **差异性自检修正**（WorkBuddy E 坑）：雷同先查角色分工失效，不盲目重跑
- 🆕 **圆桌参与司边际贡献标注**（WorkBuddy F 坑）：成本透明
- 🆕 **假设穿透综合**：3 sub-agent 各自的穿透汇总成 1 张清单
- 🆕 **落地四问归属**：主控 P4 补填，不派发子 agent
- 🆕 **异常信号按模式区分**：串行 vs 并行处理不同
- 📌 触发：錡哥 23:45「sub-agent 隔离 3 司」+ 00:50「再次仔细检查」+ 01:02「WorkBuddy 实战反馈」
- 📌 依据：錡哥 P32 v3.1.1 + WorkBuddy「三司会审 v2.8.4」实战 10 坑
- 📌 SKILL.md 主体 464 → 695 行（+231）
